export const metadata = {
    title: 'Register',
    description: 'Register your NewsNxt account',
}

export default function LoginLayout({children}) {
  return (
    <>
        {children}
    </>
  )
}
